# Copy to .env and adjust. make reads this file, so make run picks it up.
# Values must not contain a # or make truncates them.

LOG_DIR=logs
LOG_MAX_AGE_DAYS=365

# grpc listener
GRPC_HOST=0.0.0.0
GRPC_PORT=3001
# seconds to let in-flight rpcs drain on shutdown
GRPC_SHUTDOWN_TIMEOUT=15

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=hst
POSTGRES_PASSWORD=hst
POSTGRES_DB=hst
# disable is for local docker only, production must be require or verify-full
POSTGRES_SSL_MODE=disable
POSTGRES_MAX_CONN=20
POSTGRES_MIN_CONN=2

NATS_HOST=localhost
NATS_PORT=4222
NATS_NAME=hstcore

REDIS_URL=localhost:6379
REDIS_PASSWORD=
# managed redis normally requires tls
REDIS_TLS=false
REDIS_DB=0
REDIS_POOL_SIZE=
