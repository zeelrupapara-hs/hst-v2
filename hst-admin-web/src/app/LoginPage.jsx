// Placeholder until the shell phase; proves routing and the skin load.
export function LoginPage() {
  return (
    <div style={{ padding: 24 }}>
      <h3>HST Administrator</h3>
      <p>Login screen lands with the shell phase.</p>
    </div>
  );
}
